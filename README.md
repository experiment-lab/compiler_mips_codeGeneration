# MANIT
# compiler lab CSE 31
  #how to compile:
> /base  {input filename} {output
> yacc -d lang.y <br />
> lex lang.l    <br />
> gcc -ll lang.tab.c -o base<br />
  #run parser <br />
> /base  {input filename} {output filename}<br />
    or<br />
 > /base  {input filename}
